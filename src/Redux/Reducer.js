const initialState = {
    loading: false,
    currentuser:null,
    error:null,
}

const useReducer = (state = initialState,action)=> {
    switch (action.type) {
        
            
            
    
        default:
            return state
    }
}

export  default useReducer